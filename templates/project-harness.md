# Project Harness — `.doc/agents.md`

> Template. Copy to the project as `.doc/agents.md`.
> Generated by `/scope` (Project Harness Bootstrap) and refreshed by `/sync`.
>
> This file is a **manifest** over the global harness, not a copy of it. The
> global modules remain the single source of truth. Never paste their rule
> bodies here — list module paths only.
>
> Use **relative module names** (for example `standards/core.md`), never an
> absolute harness path. Each agent resolves them against its own module
> directory, so this file works for every harness (opencode, Codex, Claude Code,
> Cursor, Gemini, Windsurf, …).

## Project

```yaml
project:
  name:
  stack:
    language:
    framework:
    database:
    tooling: []
  package_manager:
  test_command:
  build_command:
  dev_command:
```

## Global Modules In Use

Load only these modules for routine work. Anything not listed is out of scope
for this project.

```text
standards/core.md
standards/agent-rules.md
standards/definition-of-done.md
standards/architecture.md
standards/authority-order.md
standards/human-review.md
standards/anti-slop.md
standards/agent-security.md
frameworks/<framework>.md
databases/<database>.md
commands/<command>.md
```

## Components (Reuse Registry)

Reusable components that already exist. Check this **before** creating a new
component — see `frontend/component-design.md`.

```text
Component            Path                          Purpose
DataTable            src/components/DataTable.tsx  Shared typed table
```

## Project Conventions

- ...

## Do Not Load

```text
INDEX.md            # consult only on explicit request
<inapplicable stack / framework modules>
```
