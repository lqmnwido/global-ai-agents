# Dependency Policy

Before adding a dependency, determine:

```text
Can the framework already do this?
Does the project already include something equivalent?
Is the package maintained?
Is the license acceptable?
Does it introduce security risk?
Does it significantly increase build size?
Is it compatible with the current framework version?
```

Dependency changes MUST be visible in the final change summary.
